apiVersion: 1

datasources:
  - name: AgentReflex API
    type: grafana-http-datasource
    access: proxy
    url: http://app:8000
    isDefault: true
    jsonData:
      httpHeaderName1: X-API-Key
    secureJsonData:
      httpHeaderValue1: ${AGENT_REFLEX_API_KEY}
